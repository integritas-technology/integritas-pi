# Session

Scratch log for the session in progress. Update it as you go; reset it when a session's work is done and merged. Not a changelog — see `CHANGELOG.md` for user-facing history.

## Progress

Branch `fix/minima-sync-missmatch`. Implemented `docs/plans/minima-rpc-console.md` end to end (backend catalog/service/routes, frontend API/panel/whitelist modal, docs). The rest of the branch's earlier work (restart/resync status, Wallet/Minima settings panels, peers-toast fix, etc.) was already committed and logged before this session started — see `git log` and `CHANGELOG.md`'s `[Unreleased] fix/minima-sync-missmatch` section for that history; not re-summarized here.

- Fetched Minima's live `help` RPC output from the running `minima` container to replace the plan's approximate ~70-command sketch with a concrete, authoritative catalog: 103 documented commands + the undocumented-but-functional `peers` (already used by existing narrow actions) = 104 real commands, minus 9 excluded = 96 catalog entries (42 read/default-on, 54 write/default-off).
- Added `backend/src/features/minima/minima-console.catalog.ts` (closed-world `ConsoleCommandEntry[]`) and `minima-console.service.ts` (`getConsoleWhitelist`/`updateConsoleWhitelist`/`runConsoleCommand`, whitelist persisted via the existing settings key/value store under `minima_console_whitelist`, re-auth via `verifyPassword` on whitelist edits, dedicated `MinimaConsoleError` class following the codebase's per-feature error-class pattern).
- Added three admin-gated routes to `minima.routes.ts`: `GET/POST /api/minima/console/whitelist`, `POST /api/minima/console/run`; whitelist edits reuse `authRateLimiter`.
- `megammrsync` and `peers action:addpeers` dispatch through the existing `resyncMegammr()`/`addMinimaPeers()` functions instead of generic passthrough, per the plan.
- Added frontend types (`MinimaConsoleCatalogEntry`, `MinimaConsoleWhitelist`, `MinimaConsoleRunResult`), `minimaConsoleApi.ts`, `MinimaConsolePanel.tsx` (terminal-style input + local-state-only scrollback), and `MinimaConsoleWhitelistModal.tsx` (checkbox list grouped read/write, PIN/password re-auth field styled after `AuthSettingsPage.tsx`'s credential-change form); wired both into a new "RPC console" `Card` on `MinimaPage.tsx` with a gear `IconButton` to open the whitelist modal.
- Updated `.claude/rules/minima.md` (+ `.agents/`/`.cursor/` counterparts), `SECURITY.md`, and added a full risk-register entry to `docs/security/host-and-infrastructure.md` documenting the console as a scoped, closed-world exception to the "no generic Minima proxy" rule.
- Updated `README.md`'s Minima API section and `CHANGELOG.md`'s `[Unreleased] fix/minima-sync-missmatch` section for the new feature.
- Verified via `npm run check` (typecheck + backend test suite, all pass), `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config`, and `docker compose build backend frontend && docker compose up -d backend frontend` (containers healthy, backend logs clean). Confirmed the three new routes are correctly wired and admin-auth-gated (401 unauthenticated) via `curl` against the running stack.
- Investigated the header status section (`AppShell.tsx`, shown on every page): it was a one-shot `fetch("/api/status/overview")` on mount with no polling, no visibility/focus refetch, and a failed fetch silently reset state to `null` — so a dropped request left the pills stuck showing "not ok" for the rest of the session with no way to tell "stale" from "actually down". The `/overview` backend route itself was already live-queried per request (no DB staleness there).
- Fixed by adding `frontend/src/features/status/useStatusOverviewRefresh.ts` (30s polling via `getStatusOverview()`/`getJson`, keeps the last known-good `overview` and surfaces a separate `error` flag instead of nulling out on a failed refresh) and wiring it into `AppShell.tsx` in place of the old one-shot effect.
- Replaced the three text pills ("Node online"/"Wallet ready"/"Integritas connected") with three clickable colored-dot indicators (`frontend/src/components/StatusDot.tsx`) labeled Node/Wallet/Integritas; clicking opens a popover with status text, service-specific detail (Node: container/sync/peer info; Wallet: MINIMA balance), a "Checked HH:MM:SS" line, and a "could not refresh, showing last known status" note when the latest poll failed. Closes on outside click.
- Added a real "wallet" entry to `GET /api/status/overview` (`status.routes.ts`) backed by `getWalletStatus()` (the existing `balance` RPC service already used by the Wallet page) — previously the header's "wallet" pill was actually just re-reading the `minima` node-status service, and the always-`ok:true` hardcoded `backend` entry is no longer surfaced in the header at all (kept in the API response, just unused there).
- Verified via `npm run check`, `npm --prefix backend run build`, `npm --prefix frontend run build`, `docker compose config` — all pass.

## Next Steps

- Commit the Minima RPC console changes (new files: `minima-console.catalog.ts`, `minima-console.service.ts`, `MinimaConsolePanel.tsx`, `MinimaConsoleWhitelistModal.tsx`, `minimaConsoleApi.ts`; modified: `minima.routes.ts`, `MinimaPage.tsx`, `app/types.ts`, docs) — typecheck/build/container-health all verified, not yet `git commit`ed.
- Commit the header status dot changes (new files: `frontend/src/components/StatusDot.tsx`, `frontend/src/features/status/useStatusOverviewRefresh.ts`; modified: `AppShell.tsx`, `statusApi.ts`, `backend/src/features/status/status.routes.ts`, `CHANGELOG.md`) — typecheck/build verified, not yet `git commit`ed. Browser click-through not done this session (see below).
- Manual browser click-through is still outstanding for the whole `fix/minima-sync-missmatch` branch, now including the console and the header status dots: toggle a few whitelist entries with correct/incorrect PIN, run a whitelisted read command, confirm a non-whitelisted command is rejected with a clear message, run `megammrsync` via console and confirm parity with the existing Resync button (banner, audit log), run `peers action:addpeers` via console and confirm it lands in the same peer list as Account Settings, and click each of the three new header status dots to confirm the popover opens/closes and shows sane detail (including simulating a dropped `/overview` request to see the "could not refresh" note). No authenticated browser session was available this session (or prior sessions on this branch) to do this; the user explicitly declined a Playwright-based automated check for the header dots this session.

## Notes / Open Questions

- The plan (`docs/plans/minima-rpc-console.md`) named 7 commands to permanently exclude from the catalog (`vault`, `sendfrom`, `signfrom`, `createfrom`, `postfrom`, `quit`, `keys`) as a representative, not exhaustive, list, and explicitly asked for reconciliation against real `help` output during implementation. Reconciling turned up two more commands with the same risk profile that got added to the exclusion list: `createtokenfrom` (same "`*from`" family, requires a raw `privatekey:` argument like the four excluded siblings) and `decryptbackup` (can turn an encrypted backup file into plaintext seed/key material). Flagging this judgment call explicitly — worth the user's sanity check since it extends the plan's stated scope by two commands.
- `sphincs` and `maxsign` both take an *optional* `privatekey:`/similar param but were kept in the catalog as write/default-off (not excluded) — reasoning: unlike the `*from` family, they're general-purpose crypto utilities that don't need the wallet's own stored key to operate, so the double-gate (admin must explicitly enable + re-auth to edit the whitelist) was judged sufficient. Revisit if that reasoning doesn't hold up.
- Architecture principle carried over from earlier in this branch: backend is the sole source of truth for Minima operation state; this feature follows the same rule by dispatching `megammrsync`/`peers action:addpeers` through the existing tracked actions rather than a parallel RPC path.
